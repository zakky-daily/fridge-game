# 逃走する冷蔵庫

深夜に逃げ出した冷蔵庫を追いかける、健康テーマの 3D Web ゲームです。

## 開発

```bash
npm install
npm run dev
```

## ビルド

```bash
npm run build
```

生成された `dist/` は GitHub Pages へそのまま配置できます。`main` ブランチへの push 時は
`.github/workflows/deploy.yml` が自動でデプロイします。
